import { CloseIcon } from './Icons';
export type Preferences={theme:'dark'|'light'|'system';restoreTerminals:'automatic'|'confirm'|'manual';notifications:boolean;reducedMotion:boolean};
export const defaults:Preferences={theme:'dark',restoreTerminals:'automatic',notifications:true,reducedMotion:false};
export function SettingsPanel({value,onChange,onClose}:{value:Preferences;onChange:(p:Preferences)=>void;onClose:()=>void}){const set=<K extends keyof Preferences>(k:K,v:Preferences[K])=>onChange({...value,[k]:v});return <div className="overlay"><section className="dialog settings"><header><div><span className="eyebrow">PRÉFÉRENCES</span><h2>Paramètres</h2></div><button onClick={onClose}><CloseIcon/></button></header><div className="setting-row"><div><strong>Apparence</strong><p>Thème de l’interface mAI</p></div><select value={value.theme} onChange={e=>set('theme',e.target.value as Preferences['theme'])}><option value="dark">Sombre</option><option value="light">Clair</option><option value="system">Système</option></select></div><div className="setting-row"><div><strong>Terminaux restaurés</strong><p>Action au redémarrage de l’application</p></div><select value={value.restoreTerminals} onChange={e=>set('restoreTerminals',e.target.value as Preferences['restoreTerminals'])}><option value="automatic">Relancer automatiquement</option><option value="confirm">Demander confirmation</option><option value="manual">Ne pas relancer</option></select></div><label className="setting-row"><div><strong>Notifications natives</strong><p>Téléchargements et erreurs importantes</p></div><input type="checkbox" checked={value.notifications} onChange={e=>set('notifications',e.target.checked)}/></label><label className="setting-row"><div><strong>Réduire les animations</strong><p>Limiter les transitions visuelles</p></div><input type="checkbox" checked={value.reducedMotion} onChange={e=>set('reducedMotion',e.target.checked)}/></label><div className="settings-note">Les paramètres sont enregistrés automatiquement sur cet appareil.</div></section></div>}
